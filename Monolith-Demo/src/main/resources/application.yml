spring:
  application:
    name: Monolith-Demo
  h2:
    console:
      enabled: true

  docker:
    compose:
      file: Monolith-Demo/docker-compose.yml
      enabled: false
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
